name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.19.0]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Important for merging full history
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm install

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 14.19.0
        uses: actions/setup-node@v3
        with:
          node-version: 14.19.0
      - run: npm install
      - run: rm -rf public

  auto_update_non_main_branches:
    needs: deploy # Only run after a successful deployment to main
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Git identity
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

      - name: Merge main to Krish
        run: |
          git checkout Krish
          git merge --no-ff main
          git push

      - name: Merge main to Carter
        run: |
          git checkout Carter
          git merge --no-ff main
          git push

      - name: Merge main to John
        run: |
          git checkout John
          git merge --no-ff main
          git push

      - name: Merge main to Ishan
        run: |
          git checkout Ishan
          git merge --no-ff main
          git push

      - name: Merge main to Will
        run: |
          git checkout Will
          git merge --no-ff main
          git push

      - name: Merge main to Nathan
        run: |
          git checkout Nathan
          git merge --no-ff main
          git push
